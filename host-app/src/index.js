import React from "react";
import ReactDOM from "react-dom/client"; // 如果是 React 18+
import App from "./App.jsx";

// 创建根节点并挂载 App
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
